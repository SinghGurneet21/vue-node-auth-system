import { Notify, Dialog, Loading } from 'quasar'
import '@quasar/extras/material-icons/material-icons.css'

export default {
  plugins: {
    Notify,
    Dialog,
    Loading
  },
framework: {
    iconSet: 'material-icons', // default icon set
  }
}
